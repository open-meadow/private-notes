To execute this program, Docker must be installed and running (either via Docker Desktop or Docker CLI)

First, clone this Git repository

Then, once Docker is running, run the following commands

```
cd <path-to-local-repo>
docker-compose up --build
```

Once the two commands are run, open a browser and go to http://localhost:5173